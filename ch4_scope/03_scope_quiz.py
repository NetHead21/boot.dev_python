# Scope Quiz
# pi = 3.14

# def get_area_of_circle(radius):
#     area = pi * radius * radius
#     return area

# Does the get_area_of_circle function have access to the pi variable?

#     Yes, because the pi variable is in the global scope
# Yes, because functions can always access any variable
# No


# Can the area variable inside the get_area_of_circle function be accessed outside of the function?

# Yes, because tomatoes are a fruit
# Yes, all variables are accessible anywhere in the program
#     No, variables defined inside a function are not accessible outside of it
